#include <bits/stdc++.h>
using namespace std;

int l;
long long n;

int main(){
    cin >> l >> n;

    for (long long i = 1; ; i ++){
        long long k = sqrt(i);
        double m = sqrt(double(i)) - k;
                - 
    }
    return 0;
}
