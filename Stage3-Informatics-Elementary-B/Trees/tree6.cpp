#include <bits/stdc++.h>
using namespace std;

int     n, r, k, v[110], lc[110], rs[110];
int     main(){
    cin >> n >> r;
    for (int i = 0; i < n; i ++){
        cin >> v[i] >> lc[i] >> rs[i];
    }

    cin >> k;
    int x, y;
    for (int i = 0; i < k; i ++){
        cin >> x >> y;
        for (int p = x; p )
    }
    return 0;
}